@model BranchUserViewModel
@{
    Layout = null;
}

<div class="modal-header">
    <h5 class="modal-title">حذف کاربر از شعبه</h5>
    <button type="button" class="btn-close" data-bs-dismiss="modal" aria-label="Close"></button>
</div>
<div class="modal-body">
    <div class="py-3 text-center">
        <i class="fa fa-user-times fa-3x text-danger mb-3"></i>
        <h4>آیا از حذف این کاربر از شعبه اطمینان دارید؟</h4>
        <p class="text-muted mb-0">
            با حذف کاربر "<strong>@Model.UserFullName</strong>" از شعبه "<strong>@Model.BranchName</strong>"، این کاربر دیگر دسترسی به این شعبه نخواهد داشت.
            <br><span class="text-warning">این عملیات قابل بازگشت است و می‌توان کاربر را مجدداً اضافه کرد.</span>
        </p>

        <div class="alert alert-info mt-3">
            <h6><i class="fa fa-info-circle me-1"></i> اطلاعات کاربر:</h6>
            <div class="text-start">
                <strong>نام:</strong> @Model.UserFullName<br>
                <strong>نقش فعلی:</strong> <span class="badge @(Model.Role == 2 ? "bg-danger" : Model.Role == 1 ? "bg-warning" : "bg-info")">@Model.RoleText</span><br>
                <strong>تاریخ اختصاص:</strong> @ConvertDateTime.ConvertMiladiToShamsi(Model.AssignDate, "yyyy/MM/dd")
            </div>
        </div>

        <div class="alert alert-warning mt-3">
            <h6><i class="fa fa-exclamation-triangle me-1"></i> نکات مهم:</h6>
            <ul class="text-start mb-0 small">
                <li>کاربر دیگر دسترسی به این شعبه نخواهد داشت</li>
                <li>تمام تسک‌های اختصاص داده شده به این کاربر در این شعبه باقی می‌ماند</li>
                <li>سابقه فعالیت کاربر در این شعبه حفظ می‌شود</li>
                <li>در صورت نیاز می‌توان کاربر را مجدداً به شعبه اضافه کرد</li>
            </ul>
        </div>
    </div>
</div>
<div class="modal-footer">
    <form method="post" action="@Url.Action("RemoveUserPost", "Branch")">
        @Html.AntiForgeryToken()
        <input type="hidden" name="id" value="@Model.Id" />

        <button type="button" class="btn btn-alt-secondary" data-bs-dismiss="modal">انصراف</button>
        <button type="submit" class="btn btn-danger" data-save="modal">
            <i class="fa fa-user-times me-1"></i> حذف از شعبه
        </button>
    </form>
</div>