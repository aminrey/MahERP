@model UserRolePatternInfo

@{
    Layout = null;
}

<style>
    .modal {
        --bs-modal-width: 70%;
        transition: 0.3s;
    }
</style>

<div class="modal-dialog" role="document">
    <div class="modal-content">
        <div class="block block-rounded block-themed block-transparent mb-0 modal-contents">
            <div class="block-header bg-danger-dark">
                <h3 class="block-title">حذف تخصیص الگوی نقش</h3>
                <div class="block-options">
                    <button aria-label="Close" class="btn-block-option" data-bs-dismiss="modal" type="button">
                        <i class="fa fa-fw fa-times"></i>
                    </button>
                </div>
            </div>

            <div class="block block-themed block-rounded mt-1">
                <div class="block-header bg-danger-light">
                    <h3 class="block-title">تأیید حذف تخصیص</h3>
                </div>
                <div class="block-content">
                    <div class="text-center py-3">
                        <div class="display-4 text-danger mb-3">
                            <i class="fa fa-user-times"></i>
                        </div>
                        <h4 class="mb-3">آیا از حذف این تخصیص اطمینان دارید؟</h4>

                        <div class="row justify-content-center">
                            <div class="col-8">
                                <table class="table table-borderless table-striped">
                                    <tr>
                                        <th class="text-end" style="width: 40%;">الگوی نقش:</th>
                                        <td class="text-start"><strong>@Model.PatternName</strong></td>
                                    </tr>
                                    <tr>
                                        <th class="text-end">تخصیص دهنده:</th>
                                        <td class="text-start">@Model.AssignedByName</td>
                                    </tr>
                                    <tr>
                                        <th class="text-end">تاریخ تخصیص:</th>
                                        <td class="text-start">@ConvertDateTime.ConvertMiladiToShamsi(Model.AssignDate, "yyyy/MM/dd")</td>
                                    </tr>
                                </table>
                            </div>
                        </div>

                        <div class="alert alert-warning mt-3">
                            <i class="fa fa-exclamation-triangle me-1"></i>
                            با حذف این تخصیص، کاربر دیگر دسترسی‌های مربوط به این الگوی نقش را نخواهد داشت.
                        </div>
                    </div>
                </div>
            </div>
        </div>

        <div class="block-content block-content-full text-end bg-body">
            <form asp-action="RemoveRolePatternPost" method="post" data-ajax="true" data-ajax-success="onRemoveSuccess" class="d-inline">
                <input asp-for="Id" type="hidden" />
                <button class="btn btn-sm btn-alt-secondary" data-bs-dismiss="modal" type="button">انصراف</button>
                <button type="submit" class="btn btn-sm btn-danger">
                    <i class="fa fa-user-times me-1"></i>حذف تخصیص
                </button>
            </form>
        </div>
    </div>
</div>

<script>
    function onRemoveSuccess(response) {
        if (response.success) {
            $('#modal-userpermission').modal('hide');
            location.reload();
        }
    }
</script>