@model StakeholderContact
@{
    Layout = null;
    var fullName = $"{Model.FirstName} {Model.LastName}";
}

<div class="modal-header">
    <h5 class="modal-title">حذف فرد مرتبط</h5>
    <button type="button" class="btn-close" data-bs-dismiss="modal" aria-label="Close"></button>
</div>
<div class="modal-body">
    <div class="py-3 text-center">
        <i class="fa fa-exclamation-triangle fa-3x text-warning mb-3"></i>
        <h4>آیا از حذف این فرد مرتبط اطمینان دارید؟</h4>
        <p class="text-muted mb-0">
            با حذف فرد مرتبط "<strong>@fullName</strong>"، تمامی اطلاعات آن از سیستم حذف خواهد شد.
            <br><span class="text-danger">این عملیات قابل بازگشت نیست!</span>
        </p>

        @if (Model.IsPrimary)
        {
            <div class="alert alert-warning mt-3">
                <i class="fa fa-exclamation-circle me-1"></i> <strong>توجه!</strong>
                این فرد به عنوان مخاطب اصلی طرف حساب تعیین شده است. با حذف آن، باید مخاطب اصلی دیگری تعیین کنید.
            </div>
        }
    </div>
</div>
<div class="modal-footer">
    <form method="post" action="@Url.Action("DeleteContactPost", "Stakeholder")">
        @Html.AntiForgeryToken()
        <input type="hidden" name="id" value="@Model.Id" />

        <button type="button" class="btn btn-alt-secondary" data-bs-dismiss="modal">انصراف</button>
        <button type="submit" class="btn btn-danger" data-save="modal">
            <i class="fa fa-trash me-1"></i> حذف
        </button>
    </form>
</div>