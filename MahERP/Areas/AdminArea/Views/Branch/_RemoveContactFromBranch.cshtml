@model MahERP.DataModelLayer.Entities.AcControl.BranchContact
@{
    Layout = null;
}

<div class="modal-dialog modal-dialog-centered">
    <div class="modal-content">
        <form asp-controller="Branch" asp-action="RemoveContactFromBranchPost" method="post" data-ajax-form="true">
            @Html.AntiForgeryToken()
            <input type="hidden" name="id" value="@Model.Id" />

            <div class="modal-header @ViewBag.ThemeClass">
                <h5 class="modal-title text-white">
                    <i class="fa fa-trash-alt me-2"></i>
                    @ViewBag.ModalTitle
                </h5>
                <button type="button" class="btn-close btn-close-white" data-bs-dismiss="modal" aria-label="Close"></button>
            </div>

            <div class="modal-body">
                <div class="text-center py-4">
                    <div class="mb-4">
                        <i class="fa fa-exclamation-triangle fa-4x text-danger"></i>
                    </div>
                    <h4 class="mb-3">حذف فرد از شعبه</h4>
                    
                    <div class="alert alert-danger">
                        <p class="mb-2">
                            آیا از حذف فرد
                            <strong class="text-dark">@ViewBag.ContactName</strong>
                            از شعبه
                            <strong class="text-dark">@ViewBag.BranchName</strong>
                            مطمئن هستید؟
                        </p>
                    </div>

                    <div class="row text-start mt-4">
                        <div class="col-md-6">
                            <div class="mb-3">
                                <small class="text-muted d-block mb-1">
                                    <i class="fa fa-building me-1"></i> شعبه:
                                </small>
                                <strong>@ViewBag.BranchName</strong>
                            </div>
                        </div>
                        <div class="col-md-6">
                            <div class="mb-3">
                                <small class="text-muted d-block mb-1">
                                    <i class="fa fa-user me-1"></i> فرد:
                                </small>
                                <strong>@ViewBag.ContactName</strong>
                            </div>
                        </div>
                    </div>

                    <div class="row text-start">
                        <div class="col-md-6">
                            <div class="mb-3">
                                <small class="text-muted d-block mb-1">
                                    <i class="fa fa-tag me-1"></i> نوع رابطه:
                                </small>
                                <strong>@Model.RelationTypeText</strong>
                            </div>
                        </div>
                        <div class="col-md-6">
                            <div class="mb-3">
                                <small class="text-muted d-block mb-1">
                                    <i class="fa fa-calendar me-1"></i> تاریخ اختصاص:
                                </small>
                                <strong>@ConvertDateTime.ConvertMiladiToShamsi(Model.AssignDate, "yyyy/MM/dd")</strong>
                            </div>
                        </div>
                    </div>

                    <p class="text-muted mt-3">
                        <i class="fa fa-info-circle me-1"></i>
                        این عملیات فرد را از شعبه حذف می‌کند اما اطلاعات فرد حفظ خواهد شد.
                    </p>
                </div>
            </div>

            <div class="modal-footer">
                <button type="button" class="btn btn-secondary" data-bs-dismiss="modal">
                    <i class="fa fa-times me-1"></i>
                    انصراف
                </button>
                <button type="button" class="btn btn-danger" data-save="modal">
                    <i class="fa fa-trash-alt me-1"></i>
                    حذف از شعبه
                </button>
            </div>
        </form>
    </div>
</div>